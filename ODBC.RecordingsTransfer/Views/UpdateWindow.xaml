<Window x:Class="ODBC.RecordingsTransfer.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Software Update"
        Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Update Available" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" x:Name="VersionText" FontSize="14" Foreground="#444444" Margin="0,0,0,12"/>

        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReleaseNotesText" TextWrapping="Wrap" FontSize="13" Foreground="#333333"/>
            </ScrollViewer>
        </Border>

        <ProgressBar Grid.Row="3" x:Name="DownloadProgress" Height="18" Margin="0,16,0,8"
                     Minimum="0" Maximum="1" Visibility="Collapsed"/>
        <TextBlock Grid.Row="3" x:Name="StatusText" Margin="0,16,0,8" FontStyle="Italic"
                   Foreground="#666666" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="LaterButton" Content="Later" Width="90" Margin="0,0,8,0"
                    Padding="10,6" Click="LaterButton_Click"/>
            <Button x:Name="InstallButton" Content="Download and Install" Width="150"
                    Padding="10,6" FontWeight="SemiBold" Click="InstallButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
